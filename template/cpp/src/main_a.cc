#include <iostream>
#include <string>

std::string solve(int N) { return std::string("Hello World"); }

int main() {
  int N;
  std::cin >> N;
  std::cout << solve(N) << std::endl;
  return EXIT_SUCCESS;
}
